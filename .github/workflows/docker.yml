name: Backend CI/CD

on:
  push:
    branches: [v4_docker]
  pull_request:
    branches: [v4_docker]

env:
  IMAGE_NAME: camweara/cloth_api_caching:v1 # 🔁 new tag to push
  BASE_IMAGE: camweara/cloth_api_caching # ⬇️ old image to pull cache from

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push new Docker tag using old tag cache
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_NAME }}
          cache-from: type=registry,ref=${{ env.BASE_IMAGE }}
          cache-to: type=inline
